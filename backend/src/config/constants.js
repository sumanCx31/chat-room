const Status = {
    ACTIVE: "active",   
    INACTIVE: "inactive",
}

const USER_ROLES = {
    ADMIN: "admin",
    USER: "user",
}

const GENDER = {
    MALE: "male",
    FEMALE: "female",
    OTHER: "other"
}

module.exports = {
    Status,
    USER_ROLES,
    GENDER
}